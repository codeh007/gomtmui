export interface Translations {
  app: {
    nav: {
      chat: string;
      sessions: string;
      analytics: string;
      logs: string;
      cron: string;
      skills: string;
      config: string;
      keys: string;
      documentation: string;
    };
    webUi: string;
  };
}
